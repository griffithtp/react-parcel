import React from 'react';
import Button from './lib/button'

export default function (props: any) {
  return (
    <div>
      app with parcel
      <Button>
        Click me
      </Button>
    </div>
  )
}